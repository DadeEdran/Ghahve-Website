node_modules
web/.next
server/uploads
package-lock.json

# نسخهٔ ایستای اولیه — اجرا نمی‌شود (مورد ۱۸ سند ضعف‌ها)
/script.js
/index.html
/style.css
/img

# داده‌های seed: هر ردیف عمداً یک خط بلند است تا کاتالوگ
# در یک نگاه خوانده شود؛ شکستنشان خوانایی را کم می‌کند.
server/src/data/seed-items.js
server/src/data/seed-enrich.js

# خروجی ساخته‌شدهٔ مستندات — دست‌نویس نیست، هر بار از نو تولید می‌شود
# (npm run docs:pdf). فونت‌ها به‌صورت base64 داخلش‌اند و چند صد کیلوبایت
# رشتهٔ یک‌خطی دارد؛ قالب‌بندی‌اش نه معنا دارد نه شدنی است.
docs/documentation.html

# ساخت PDF مستندات: کدِ چیدمان است، نه منطق. مختصات SVG، جدول رنگ‌ها و
# بلوک CSS دستی ستون‌به‌ستون هم‌تراز شده‌اند تا موقع جابه‌جا کردن یک برچسب
# بشود عددها را با چشم مقایسه کرد. prettier همهٔ آن ستون‌ها را می‌شکند.
# (render.mjs و zoom.mjs عمداً بیرون این فهرست‌اند و قالب‌بندی می‌شوند،
#  و هر چهار فایل زیر همچنان زیر پوشش eslint هستند.)
scripts/docs/build.mjs
scripts/docs/diagrams.mjs
scripts/docs/paginator.js
scripts/docs/check-svg.mjs

# مستندات فارسی.
# تنها کاری که prettier با این فایل‌ها می‌کند، هم‌تراز کردن
# ستون‌های جدول‌هاست — و چون متن راست‌به‌چپ است و پهنای
# حرف‌های فارسی با شمارش نویسه‌ها جور درنمی‌آید، نتیجه در
# ویرایشگر به‌هم‌ریخته‌تر از حالت فشردهٔ فعلی می‌شود.
# راه دیگر، گذاشتن <!-- prettier-ignore --> بالای هر جدول
# بود که در ۱۴ فایل بیش از صد کامنت می‌شد.
*.md
