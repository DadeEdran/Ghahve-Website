import Link from 'next/link';

/* ══════════════════════════════════════════════════
   آدرسی که هیچ صفحه‌ای پشتش نیست.

   ── تفاوت با نسخهٔ ویت، عمدی ──
   آنجا آخرین سطر جدول مسیرها <Navigate to="/"> بود:
   هر آدرس ناشناخته بی‌صدا به صفحهٔ اصلی می‌رفت و پاسخ
   **۲۰۰** می‌گرفت. برای موتور جست‌وجو یعنی «این آدرس
   وجود دارد و محتوایش همان صفحهٔ اصلی است» — همان
   soft 404 ای که برای صفحهٔ کالا کنار گذاشته شده بود.

   حالا پاسخ ۴۰۴ است. صفحهٔ کالا از این استفاده نمی‌کند؛
   متن مخصوص خودش را دارد (app/_item/ItemNotFound.jsx).
   ══════════════════════════════════════════════════ */

export const metadata = {
  title: 'صفحه پیدا نشد — رُست‌خانهٔ دانه',
  robots: { index: false, follow: true }
};

export default function NotFound() {
  return (
    <main className="item-page">
      <div className="wrap">
        <div className="load-error" role="alert">
          <h1>این صفحه پیدا نشد</h1>
          <p>شاید آدرس را ناقص کپی کرده باشید. از صفحهٔ اصلی دوباره شروع کنید.</p>
          <Link className="btn btn-primary" href="/">
            بازگشت به فروشگاه
          </Link>
        </div>
      </div>
    </main>
  );
}
