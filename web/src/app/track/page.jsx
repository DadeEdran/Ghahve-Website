import { Suspense } from 'react';
import { SITE_NAME } from '@ghahve/shared/seo.js';
import { toNextMetadata } from '../../lib/metadata.js';
import Track from '../../components/Track.jsx';

/* ══════════════════════════════════════════════════
   پیگیری سفارش (مورد ۳۵).

   ── چه چیزی روی سرور می‌ماند و چه چیزی نه ──
   خودِ پیگیری نمی‌تواند سروری باشد: مشتری باید شمارهٔ
   سفارش و موبایلش را بنویسد و آن جفت هیچ‌وقت در آدرس
   نمی‌نشیند (نه باید بنشیند). پس فرم و نتیجه سمت مشتری
   می‌مانند، همان‌طور که بودند.

   آنچه سروری شد، تگ‌های <head> است. در نسخهٔ ویت
   useHead آن‌ها را در زمان اجرا می‌نوشت؛ حالا در همان
   پاسخ اول هستند — که برای noindex مهم است: رباتی که
   جاوااسکریپت اجرا نمی‌کند هم آن را می‌بیند.

   ── چرا Suspense ──
   کامپوننت مشتری useSearchParams صدا می‌زند (برای
   پیش‌پر کردن کد از لینکِ رسید). Next می‌خواهد چنین
   تکه‌ای مرز Suspense خودش را داشته باشد تا اگر روزی
   این مسیر پیش‌رندر شد، بقیهٔ صفحه گروگانِ پارامترهای
   آدرس نماند.
   ══════════════════════════════════════════════════ */

export const metadata = toNextMetadata({
  title: `پیگیری سفارش — ${SITE_NAME}`,
  description: 'وضعیت سفارشتان را با شمارهٔ سفارش و شمارهٔ موبایل ببینید.',
  /* همان چیزی که robots.txt هم می‌گوید — ولی این یکی
     ایندکس شدن را هم می‌بندد، نه فقط خزیدن را. */
  robots: 'noindex, follow'
});

export default function TrackPage() {
  return (
    <Suspense fallback={<div className="admin-boot">در حال آماده شدن…</div>}>
      <Track />
    </Suspense>
  );
}
